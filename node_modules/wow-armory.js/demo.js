const Armory = require('./armory');
const armoryClass = new Armory('bnet_id', 'bnet_serect', 'bnet_region');

armoryClass.getCharacter('region', 'server', 'character', 'optional_query_params')
    .then((characterInfo) => {
        console.log(characterInfo);
    });